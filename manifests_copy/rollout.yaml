apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: gopal-bg-demo
  namespace: default
spec:
  replicas: 2
  strategy:
    blueGreen: 
      activeService: gopal-prod
      previewService: gopal-preview
      autoPromotionEnabled: false
  selector:
    matchLabels:
      app: gopal-bg
  template:
    metadata:
      labels:
        app: gopal-bg
    spec:
      containers:
      - name: rollout-demo
        image: argoproj/rollouts-demo:green     # chnaged the image from blue(current) to green(the updated)
        ports:
        - containerPort: 8080